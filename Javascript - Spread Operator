//Spread operators
//Allow an iterable such as an array or string to be expanded in places where zero or more args are expected
//unpacks elements

/*
let numbers = [49, 1, 2, 3, 4, 5, 6, 7, 8];
let maximum = Math.max(...numbers);//will display the max num 
console.log(maximum);
*/

let class1 = ['Jim', 'Adam', 'Sam'];
let class2 = ['Alex', 'Cindy', 'Anna'];

class1.push(...class2);

console.log(class1);
