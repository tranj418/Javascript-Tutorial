//toLocaleString() = returns a string with a language sensitive representation of this number
//handy if you need to convert currency to local

//number.toLocalString(locale, {options})

//'locale' parameter specify the language (undefined = default)
//'options' = object with formatting options

let myNum = 25658.892;
let myPercent = .06;
let myUnits = 86

//myNum = myNum.toLocaleString('en-us');//US English
//myNum = myNum.toLocaleString('hi-IN');// Hindi
//myNum = myNum.toLocaleString('de-DE');// German


//To convert to local currency in other nations
//myNum = myNum.toLocaleString("en-US", {style: 'currency', currency: 'USD'});
//myNum = myNum.toLocaleString("hi-IN", {style: 'currency', currency: 'INR'});
//myNum = myNum.toLocaleString("de-DE", {style: 'currency', currency: 'EUR'});

//Percentages
//myPercent = myPercent.toLocaleString(undefined, {style: 'percent'});


//units

myUnits = myUnits.toLocaleString(undefined, {style: 'unit', unit: 'celsius'})

console.log(myUnits)
